# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently a blank slate: it contains only a README.md (project title "Project1") and no source code, build configuration, dependency manifests, or CI workflows yet.

## Current State

- **No build/lint/test commands exist yet.** There is no package manifest (package.json, pyproject.toml, Cargo.toml, etc.), so there is nothing to build or test at this time.
- **No architecture to document yet.** The codebase has no source files.
- The default branch is `master`.

## Guidance for Future Work

- When code is first added to this repository, update this file with the actual build, lint, and test commands, and a description of the emerging architecture.
- Do not assume a language or toolchain; check what has been added to the repository before running commands.
